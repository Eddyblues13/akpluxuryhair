# Base URL of the Laravel API (no trailing slash).
VITE_API_URL=http://localhost:8000/api

# WhatsApp line orders are handed off to (digits only, with country code).
VITE_WHATSAPP_NUMBER=2348000000000
